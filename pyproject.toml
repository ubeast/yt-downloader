[project]
name = "yt-downloader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Michael Schertz", email = "michael.schertz@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "yt-dlp>=2026.8.19",
]

[project.scripts]
yt-downloader = "yt_downloader:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
